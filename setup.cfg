[metadata]
description_file = README.md
license_files = LICENSE

[tool:pytest]
testpaths = tests
addopts = -v --tb=short
markers =
    hardware: test requires real hardware sensors (skipped in CI)
filterwarnings =
    ignore:urwid\.\w+ is moved to:DeprecationWarning
